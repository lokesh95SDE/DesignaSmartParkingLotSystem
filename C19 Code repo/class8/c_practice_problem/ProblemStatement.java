package class8.c_practice_problem;

/**
 * ============================================================================
 * PRACTICE PROBLEM: Online Food Delivery System
 * ============================================================================
 * Time: ~60 minutes
 *
 * Build a simplified food delivery system that supports the following:
 *
 * 1. A restaurant has a menu with items. Each item has a name, price,
 *    and a food type (veg, non-veg, or beverage).
 *
 * 2. A customer can browse a restaurant's menu and place an order
 *    with one or more items (each with a quantity).
 *
 * 3. The system should calculate the total bill for an order.
 *
 * 4. The system should support at least two different payment methods
 *    (e.g., credit card, UPI). Each payment method should have its own
 *    validation rules. After successful payment, a receipt is generated.
 *
 * 5. A delivery agent can be assigned to an order. The agent picks up
 *    the order and delivers it.
 *
 * 6. An order goes through a lifecycle: placed → confirmed → preparing
 *    → out for delivery → delivered.
 *
 * 7. Both the order and the delivery agent should support "tracking" —
 *    i.e., you should be able to ask either of them for their current
 *    status as a string.
 *
 * 8. Write a Main class that simulates a complete flow:
 *    create a restaurant, add menu items, create a customer, place an
 *    order, pay for it, assign a delivery agent, move through statuses,
 *    and show tracking info.
 *
 * --- GOALS ---
 *    - Apply the OOP concepts and design principles you have learned
 *    - Think about which classes you need and how they relate to each other
 *    - Think about ownership: which objects create/own other objects vs.
 *      which objects just reference each other?
 *    - Use abstraction and polymorphism where it makes sense
 *    - Keep each class focused on a single responsibility
 *
 * SOLUTION: See the 'solution' subpackage (try on your own first!)
 */
public class ProblemStatement {
    // This file is intentionally left as documentation only.
    // Students should create their own solution before looking at the solution package.
}
